package com.example.bot.spring;

public class ProjectUnknownController {

    public ProjectUnknownController() {}
    
    public HandleUnknown (String text) { //input can also be the buffer
    //TODO: insert text into Question table in database, by calling SQLDatabaseEngine.
    //Need to know user's LineID
    
    }
    
}